# pg_embedding_gen-by-yhw
A PostgreSQL extension that generates text embeddings using configurable external models (like BGE-M3 or OpenAI) via a Python proxy, returning vector arrays directly from SQL functions.
